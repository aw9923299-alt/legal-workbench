# AGENTS.md — Codex 开发说明

本文件是 Codex 及其他代码 Agent 在本仓库工作的首要说明。修改代码前应先阅读本文件、`README.md` 和 `docs/CODEX_HANDOFF.md`。

## 1. 项目目标

“法务工作台”是一套以飞书消息为入口、以 AI 管家为核心、以法务任务闭环为主线的企业级法务工作系统。它不是普通待办工具，也不是一个自动给出法律结论的聊天机器人。

核心链路：

```text
授权消息接入 → 上下文聚合 → 法务事项识别 → 人工确认 → 任务执行
→ 等待与提醒 → 专业 Agent 调用 → 结案归档 → 人工反馈
```

## 2. 当前仓库状态

当前版本是 **React 前端高保真原型**：

- 已有今日工作台、AI 收件箱、任务中心、任务详情、事项库、Agent 中心、数据与权限页面；
- 使用 Mock 数据；
- 已建立 `FeishuAdapter`、`TaskRepository`、`AgentGateway` 的初始接口；
- 尚未接入真实飞书、后端数据库、模型服务、鉴权、审计存储或文件解析；
- 页面中的“同步正常”“调用 Agent”等交互均是演示状态，不能描述为生产能力。

## 3. 开发命令

```bash
npm install
npm run dev
npm run typecheck
npm run build
npm run preview
```

最低建议环境：Node.js 20 LTS 或更高版本。

## 4. 架构边界

### 页面层

`src/pages` 与 `src/components` 只负责展示、交互和页面状态，不得直接写入飞书 SDK、模型 SDK 或数据库调用。

### 领域层

`src/types` 保存共享领域模型。新增字段前应检查：

- 是否属于任务事实，而非临时 UI 状态；
- 是否需要区分 AI 提取值与人工确认值；
- 是否需要来源、时间、操作者和审计信息；
- 是否涉及敏感数据和字段权限。

### 服务适配层

外部能力必须通过接口接入：

- 飞书：消息、线程、文件、用户、权限和增量同步；
- 任务服务：创建、更新、合并、状态迁移、提醒和审计；
- Agent 服务：注册、调用、审批、重试、取消、结果回写和成本；
- AI 服务：识别、抽取、分类、关联、摘要和风险建议。

页面不得依赖具体供应商返回结构。

## 5. 不可破坏的领域规则

1. 低置信度消息不得直接创建正式任务，只能进入待确认收件箱或任务草稿。
2. 每个 AI 判断必须保留来源、依据、模型/规则版本、置信度和生成时间。
3. 人工确认信息不得被后续 AI 静默覆盖。
4. 任务状态必须与当前行动人、下一步行动、等待对象、等待内容和提醒时间保持一致。
5. 任务合并、拆分、关闭、删除、风险升级和 Agent 写回必须可审计、可追溯。
6. 高风险外发、文件删除、权限变更和正式法律意见默认需要人工审批。
7. 系统不得绕过飞书原始数据权限，也不得默认读取全部会话。
8. Mock 与真实适配器必须可切换；不得在页面组件中写死真实凭据或外部接口。

## 6. UI/UX 原则

- 桌面端优先，重点覆盖 1440px 与 1920px；
- 高信息密度，但保持明确的标题、状态和操作层级；
- AI 内容、人工内容、系统记录必须有稳定且一致的视觉区别；
- 重要风险使用局部警示，不使用大面积高饱和红色；
- 长文本必须处理换行、省略、Tooltip 或横向滚动；
- 所有异步页面必须具备加载、空、失败、无权限和重试状态；
- 批量及破坏性操作必须显示影响范围并支持二次确认。

## 7. 安全与隐私约束

- 前端只保存公开配置；密钥、App Secret、模型 Key 必须由后端持有；
- 日志不得输出原始私聊、合同全文、身份证号、手机号或访问令牌；
- 测试数据不得复制真实公司敏感信息；
- 新增 Agent 时必须声明读取范围、输出位置、风险级别和审批策略；
- 文件解析与提示词输入需考虑恶意内容、提示注入和越权数据引用。

## 8. 推荐开发顺序

1. 建立路由、状态管理和统一请求层；
2. 补齐领域模型、状态机和审计事件模型；
3. 将页面从直接读取 Mock 改为通过服务/查询层读取；
4. 实现本地 Mock API 或 BFF 契约；
5. 实现 AI 收件箱确认闭环；
6. 实现任务详情编辑、状态迁移与时间线；
7. 接入飞书 OAuth、授权范围与增量同步；
8. 接入 AI 识别和任务关联；
9. 接入 Agent 注册、审批和回写；
10. 完善权限、审计、测试、监控与部署。

详细优先级见 `docs/ROADMAP.md`。

## 9. 每次提交的完成标准

- 说明本次改动解决的用户问题；
- 不绕过适配层引入外部调用；
- TypeScript 无错误，`npm run build` 通过；
- 新交互具备加载、空、错误和权限状态；
- 对关键领域规则补充测试；
- 更新相关文档、Mock 和验收标准；
- 不提交密钥、真实聊天数据、构建产物或 `node_modules`。

## 10. 首批建议任务

Codex 首次继续开发时，优先处理 `docs/ROADMAP.md` 中的 P0：

- 接入 React Router；
- 建立统一的应用服务和请求状态；
- 完善任务与消息领域模型；
- 实现 AI 收件箱的确认/关联/忽略状态流；
- 实现任务状态机和变更审计；
- 为核心逻辑添加测试。
